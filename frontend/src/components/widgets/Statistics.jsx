export const Statistics = ({ className }) => {
    return (
        <div className={`w-64 h-64 flex items-center justify-center bg-tertiary-500 text-white text-2xl text-center font-semibold rounded-3xl transform shadow-xl ${className}`}>
            Grafico de estadisticas
        </div>
    );
};

export default Statistics;